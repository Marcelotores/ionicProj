import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SecoundPage } from './secound';

@NgModule({
  declarations: [
    SecoundPage,
  ],
  imports: [
    IonicPageModule.forChild(SecoundPage),
  ],
})
export class SecoundPageModule {}
